import React from 'react';
import { Amount } from '@vakif/amount';

export default {
	title: 'Components/Amount',
	component: Amount,
};
export const Primary = () => {
	return (
		<div>
			<Amount />
		</div>
	);
};
